export async function buyNft(id: number | string) {
  // Placeholder for integrating a blockchain contract call
  // eslint-disable-next-line no-alert
  alert(`Buying NFT ${id}`);
  console.log(`Buying NFT ${id}`);
}
