// Placeholder chain integration for Hyperliquid
export function initChainIntegration() {
  // No-op initializer; replace with target chain wiring when available
  return Promise.resolve();
}

export async function verifyNFTOwnership(_userAddress: string, _tournamentId: string) {
  // Always return false until a replacement integration is supplied
  return false;
}
