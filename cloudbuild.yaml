steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "${_IMAGE_URI}",
        "--build-arg",
        "BUILD_TARGET=${_BUILD_TARGET}",
        ".",
      ]
images:
  - "${_IMAGE_URI}"
